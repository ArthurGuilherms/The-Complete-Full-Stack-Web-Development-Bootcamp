import { randomSuperhero } from 'superheroes';
import generateName from 'sillyname';

const sillyName = generateName();
console.log(`My super hero name is ${randomSuperhero()} ${randomSuperhero()}!`);